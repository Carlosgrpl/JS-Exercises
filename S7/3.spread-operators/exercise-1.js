const pointsList = [32, 54, 21, 64, 75, 43];

/* Los 3 puntos son el spread operator, lo que hace poner eso antes del nombre de la variable es desagregar el array */

const pointsListCopy = [...pointsList];
/* Así copiamos el array */
