const toy = { name: "Bus laiyiar", date: "20-30-1995", color: "multicolor" };

const toyCopy = { ...toy };

console.log(toyCopy);

/* No se suele usar el spread operator con objetos, porque no es una buena practica y es mejor usar el operador de propagación */
